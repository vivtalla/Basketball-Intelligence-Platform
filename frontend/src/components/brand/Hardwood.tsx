/**
 * Hardwood — re-exports the shared procedural wood-grain SVG used as a
 * tonal background on hero and broadsheet panels. Source of truth lives
 * in the top-level <HeroHardwood>; this file gives the brand directory
 * a single import surface alongside the other primitives.
 */
export { default } from "@/components/HeroHardwood";
export type { default as HardwoodProps } from "@/components/HeroHardwood";
