/**
 * Reveal — re-exports the shared IntersectionObserver fade-up wrapper.
 * Source of truth lives in the top-level <Reveal>; this file gives the
 * brand directory a single import surface alongside the other primitives.
 */
export { default } from "@/components/Reveal";
