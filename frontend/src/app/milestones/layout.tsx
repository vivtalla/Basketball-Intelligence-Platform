// Sprint 88 (D) — ISR for /milestones. Daily-synced; 1hr cache.
export const revalidate = 3600;

export default function MilestonesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
