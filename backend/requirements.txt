fastapi==0.124.4
starlette==0.44.0  # paired with fastapi; pinned explicitly so deploy can't drift
uvicorn[standard]==0.33.0
gunicorn==23.0.0
nba_api==1.5.2
pandas>=2.2.0,<3.0  # 2.0.3 fails to build on Python 3.12 (pkg_resources missing)
pydantic==2.10.6
python-dotenv==1.0.1
sqlalchemy==2.0.49
psycopg2-binary==2.9.10
alembic==1.14.1
pypdf==5.9.0
# Sprint 81 — Spotrac / PST / Sports Reference scrapers depend directly on
# requests + beautifulsoup4. Both were transitive deps via nba_api but the
# scrapers should pin them explicitly.
requests==2.32.4
beautifulsoup4==4.12.3
playwright>=1.40.0
slowapi>=0.1.9
# Sprint 98 Stream A3 — structured logging with request-ID + cron RUN_ID
# propagation so a single failing cron tick can be traced end-to-end.
structlog>=23.1.0
# Sprint 98 Stream A4 — error tracking for FastAPI + cron scripts. Free tier
# (5K events/mo) is sufficient at current traffic. SENTRY_DSN gated; unset
# means no-op so dev environments are unaffected.
sentry-sdk[fastapi]>=2.14.0
# Sprint 78 CF1 — share-card rendering uses PIL (Pillow). Has been working
# locally + on the VM as a transitive dep of something in the install graph,
# but isn't actually declared anywhere — surfaced when Sprint 98 CI ran from
# a clean install and couldn't import. Pinning explicitly.
Pillow>=10.0.0
# Sprint 100 (Stream D) — process + system memory snapshot for /api/health.
# Wrapped behind utils/memory_stats.py so an unavailable psutil degrades
# gracefully to ``{"error": "unavailable"}`` rather than 500.
psutil>=5.9.0
